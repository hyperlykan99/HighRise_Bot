ROLLBACK LAST SYSTEM FALLBACK UPDATE. RESTORE LAST WORKING COMMAND STATE.

Problem:
After the last update, every command stopped responding again:
- /systemresponder
- /bots
- /bothealth
- /deploymentcheck
- /help
- /commandtest
- /safeboot status
- /crashlogs latest

Before the last update, these were working:
- /bal worked from BankingBot
- /pokerhelp worked from ChipSoprano
- /minehelp worked from GreatestProspector
- /crashlogs latest worked
- /safeboot status worked
- /commandtest partially worked

Goal:
Rollback the last “single system fallback responder / online detection” changes only.
Restore the previous working command state.

DO NOT touch:
- poker logic
- blackjack logic
- mining logic
- banker balance logic
- database schema
- AI
- autogames
- background loops
- bot tokens

Keep SAFE_BOOT on.

==================================================
PART 1: RESTORE MESSAGE HANDLER
==================================================

Make sure the first chat/message handler is active again.

Before any router, permission check, ownership check, database check, or SAFE_BOOT check:

Log every command to console:
[RX] bot=<mode> user=<username> text=<message>

If text starts with "/", process it.

Do not ignore owner messages.
Only ignore messages from the bot itself.

==================================================
PART 2: RESTORE EMERGENCY COMMANDS THAT WORKED
==================================================

Hardcode these commands before normal router:

/ping
/ping all
/crashlogs latest
/safeboot status

They must work even if Host is missing.

Temporary rule:
Allow multiple bots to answer these again if needed.
Do not attempt single responder logic yet.

Working is more important than duplicate replies right now.

Expected:
/ping all -> project bots reply with id/mode/token_source
/crashlogs latest -> no crash logs found OR latest crash
/safeboot status -> SafeBoot env/db/mode

==================================================
PART 3: RESTORE MODULE COMMAND ROUTING
==================================================

Make sure these module commands work again:

Banker:
- /bal
- /bankerhelp

Poker:
- /pokerhelp
- /pt
- /poker state

Miner:
- /minehelp
- /mine
- /ores

Blackjack:
- /bjhelp
- /rbjhelp

Do not route these through Host.
Each module bot should handle its own commands.

==================================================
PART 4: DISABLE NEW SINGLE RESPONDER LOGIC
==================================================

Temporarily disable/remove the new logic from the last update:

- get_system_responder_mode()
- single fallback responder enforcement
- online detection changes that make bots ignore commands
- any command gate that says “if not responder, return” before checking module commands

Reason:
That logic caused every command to stop responding.

Do not use single-responder enforcement until command core is stable.

==================================================
PART 5: COMMAND ORDER MUST BE SIMPLE
==================================================

Message handler order:

1. Receive message
2. Log [RX]
3. Ignore only if from self
4. If /ping, /ping all, /crashlogs latest, /safeboot status -> handle immediately
5. If module command belongs to this bot -> handle it
6. If unknown -> ignore or Host fallback if available

Do not let SAFE_BOOT block command handling.

SAFE_BOOT disables background loops only.

==================================================
PART 6: TESTS
==================================================

After restart, test only:

/ping all
/crashlogs latest
/safeboot status
/bal
/pokerhelp
/minehelp
/bjhelp

Expected:
- At least these commands respond again.
- Duplicate emergency replies are acceptable for now.
- No need to fix /help or /commandtest yet.

Final requirement:
Restore the previous working state first. Do not try to make system fallback perfect yet.