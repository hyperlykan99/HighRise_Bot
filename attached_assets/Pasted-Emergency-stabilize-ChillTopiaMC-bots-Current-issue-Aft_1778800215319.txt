Emergency stabilize ChillTopiaMC bots.

Current issue:
After the latest update, bots join the room, leave, and keep restarting.

Suspect commit:
55aef6e — Enhance bot with jail enforcement and Luxe Ticket administration features.

Goal:
Make all bots stable first. Disable risky jail enforcement and Luxe admin startup imports until confirmed safe.

Do this immediately:

1. Temporarily disable these imports in main.py:

from modules.jail_enforcement import (
    enforce_jail_on_rejoin, is_jailed,
    jail_block_message, TELEPORT_BLOCKED_CMDS,
)

from modules.luxe_admin import (
    handle_addtickets, handle_removetickets, handle_settickets, handle_sendtickets,
    handle_ticketadmin, handle_ticketlogs, handle_ticketbalance,
)

Replace them with safe lazy imports inside handlers only.

2. Temporarily disable jail teleport block in main.py:

if cmd in TELEPORT_BLOCKED_CMDS and is_jailed(user.id):
    await self.highrise.send_whisper(user.id, jail_block_message(user.id))
    return

Comment it out for now.

3. Temporarily disable startup jail recovery:

_safe_task(startup_jail_recovery(self), "startup_jail_recovery")

Comment it out until database tables are confirmed.

4. Keep the base jail command system only if it does not crash:
- !jail
- !bail
- !jaildebug
- !setjailspot

But do not run background jail recovery yet.

5. Add crash-safe guards:

try:
    from modules.jail_enforcement import is_jailed, jail_block_message, TELEPORT_BLOCKED_CMDS
except Exception as e:
    print(f"[SAFE IMPORT] jail_enforcement disabled: {e!r}")
    TELEPORT_BLOCKED_CMDS = frozenset()
    def is_jailed(user_id): return False
    def jail_block_message(user_id): return "Jail system temporarily disabled."

try:
    from modules.luxe_admin import (
        handle_addtickets, handle_removetickets, handle_settickets, handle_sendtickets,
        handle_ticketadmin, handle_ticketlogs, handle_ticketbalance,
    )
except Exception as e:
    print(f"[SAFE IMPORT] luxe_admin disabled: {e!r}")

6. Confirm database migration exists before enabling jail recovery.

database.py must create these tables during init_db():

CREATE TABLE IF NOT EXISTS jail_sentences (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    target_user_id TEXT NOT NULL,
    target_username TEXT NOT NULL,
    jailed_by_user_id TEXT NOT NULL,
    jailed_by_username TEXT NOT NULL,
    start_ts REAL NOT NULL,
    end_ts REAL NOT NULL,
    duration_seconds INTEGER NOT NULL,
    remaining_seconds INTEGER DEFAULT 0,
    jail_reason TEXT DEFAULT '',
    jail_source TEXT DEFAULT 'player',
    status TEXT DEFAULT 'active',
    bail_cost INTEGER DEFAULT 0,
    created_at TEXT DEFAULT (datetime('now')),
    updated_at TEXT DEFAULT (datetime('now'))
);

CREATE TABLE IF NOT EXISTS jail_logs (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    action TEXT NOT NULL,
    target_user_id TEXT,
    target_username TEXT,
    actor_user_id TEXT,
    actor_username TEXT,
    amount INTEGER DEFAULT 0,
    details TEXT DEFAULT '',
    timestamp TEXT DEFAULT (datetime('now'))
);

7. Do not allow missing jail tables to crash startup.
In startup_jail_recovery:
- wrap all DB reads in try/except
- if jail_sentences table missing, print warning and return
- never kill bot startup

8. Remove database files from Git tracking:
- artifacts/highrise-bot/casino_bot.db
- artifacts/highrise-bot/casino_bot.db-shm
- artifacts/highrise-bot/casino_bot.db-wal

Add to .gitignore:
*.db
*.db-shm
*.db-wal

9. Run before deploy:
python -m py_compile main.py
python -m py_compile modules/jail_enforcement.py
python -m py_compile modules/jail_system.py
python -m py_compile modules/luxe_admin.py
python -m py_compile modules/gold_tips.py

10. Restart and verify:
- bots stay online for at least 2 minutes
- no fast-exit loop
- Replit logs do not show ImportError, NameError, sqlite OperationalError, or missing table error

Final output:
Report:
1. Exact crash traceback found
2. Files changed
3. Jail enforcement disabled or safely guarded
4. Luxe admin import guarded
5. Jail DB migration added
6. DB files removed from Git tracking
7. Py_compile result
8. Bot stability test result